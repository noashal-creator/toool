# חלון התוצאה — הפרופורציות שאני אוהבת

מסמך להעברה לצ'אט אחר. כל המספרים נמדדו מול הפיגמה (node `2949:83`) ואומתו בדפדפן.

---

## 1. הכלל החשוב ביותר

**החלון הוא 1207 × 975 — אבל אסור לכתוב את זה בפיקסלים.**

כל האתר הוא פריים פיגמה של **1728 × 1080** שנמתח לגודל הדפדפן דרך יחידת עיצוב אחת:

```css
--s: calc(100dvh / 1080);   /* יחידת המידה של כל האתר */
```

הסרגל הוא `352 * --s`, הקערה `240 * --s`, וכן הלאה. **החלון חייב להיכתב באותה יחידה**, אחרת הוא מצויר בקנה מידה אחר מכל מה שסביבו ונראה שגוי — גם אם המספר "נכון".

```css
width:  calc(1207 * var(--u));
height: calc(975  * var(--u));
```

### איך יודעים שזה נכון
היחס בין החלון לסרגל חייב לצאת **3.43** (=1207/352). אם הוא יוצא 3.81 — החלון נכתב ביחידה הלא נכונה.

---

## 2. הבעיה שחוזרת: מסך מלא

`--s` נגזר **מהגובה בלבד**. במסך מלא סרגל הדפדפן נעלם → הגובה גדל → הכל גדל, כולל **הרוחב** של החלון — אבל המסך לא התרחב, אז החלון נחתך.

**הפתרון (לא לוותר על היחידה!):** תקרה שנכנסת רק כשהחלון היה עומד לחרוג:

```css
--fit: min(calc((100vw - var(--sbw) - 4vw) / 1207),
           calc((100dvh - 4vh) / 975));
--u:   min(var(--s), var(--fit));
```

גורם אחד (`--u`) מניע את **שני** הצירים, ולכן היחס 1207:975 לא יכול להתעוות. בגודל רגיל `--s` תמיד קטן יותר ולכן הוא שמנצח — כלומר החלון בדיוק 1207×975 כמתוכנן, והתקרה מתערבת רק ברגע החריגה.

**שני הצירים חייבים להישאר אורך מוגדר.** אסור `height: auto` — האזור העליון מוגדר כאחוז מהגובה הזה, וזה יוצר תלות מעגלית שמקריסה את החלון לגמרי.

---

## 3. החלוקה הפנימית

| חלק | ערך |
|---|---|
| אזור התמונה (hero) | **71.18%** מגובה הכרטיס (הקו בפיגמה ב-y=694 מתוך 975) |
| רצועת 5 התמונות | שאר הגובה, `grid` של 5 עמודות שוות |
| רקע | `#f0f0f0` על **כל** הכרטיס |
| מסגרת | `1px dashed #000` |
| פינות | `border-radius: 0.9cqw` |

**כל המידות בפנים ב-`cqw`** (הכרטיס הוא `container-type: size`), כך ש-1cqw = 1% מרוחב הכרטיס. זה אומר שכל המספרים מהפיגמה נשארים נכונים בכל גודל: 25px → `2.07cqw`, 16px → `1.33cqw` וכו'.

---

## 4. המיקום על המסך

```css
.mixwin {
  position: fixed; inset: 0;
  display: flex; align-items: center; justify-content: center;
  padding: 2.5vh 2.5vw;
  padding-left: var(--sbw);   /* ממורכז באזור שמימין לסרגל */
}
```

השוליים מסביב **גדולים ולא זהים** — זה מכוון, לא באג. אין לתקן את זה ל"מאוזן".

---

## 5. טעויות שכבר עשינו (לא לחזור עליהן)

1. **פיקסלים קבועים** (`width: 1207px`) — יוצא ענק ולא מסונכרן עם האתר.
2. **מדידה לפי רוחב בלבד** (`69.85vw`) — מייצב את המסך המלא אבל שובר את היחס לסרגל (יצא 3.81 במקום 3.43, וגבוה מהמסך).
3. **`min()` על הרוחב בלבד** — משנה את הגודל בכל שינוי חלון.
4. **`height: auto` עם `aspect-ratio`** — מקריס את החלון לאפס (תלות מעגלית עם ה-71.18%).
5. **לאזן/למרכז את השוליים** — הורס את הקומפוזיציה המכוונת.

---

## 6. בדיקה מהירה

בקונסול, כשהחלון פתוח:

```js
var c = document.querySelector('.mixwin__card');
var sb = document.querySelector('.sidebar');
console.log({
  size:   c.offsetWidth + '×' + c.offsetHeight,        // צריך 1207×975 ביחס לעיצוב
  aspect: (c.offsetWidth / c.offsetHeight).toFixed(3), // צריך 1.238
  vsSidebar: (c.offsetWidth / sb.offsetWidth).toFixed(2) // צריך 3.43
});
```

ובדיקת המסך המלא: לשנות רק את **גובה** החלון (אותו רוחב) — הגודל **לא אמור להשתנות**.
